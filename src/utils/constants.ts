export const DEFAULT_USER_MEDIA_OPTS: MediaStreamConstraints = {
    audio: true,
    video: true,
}

export const DEFAULT_DISPLAY_MEDIA_OPTS: DisplayMediaStreamOptions = {
    video: true,
    audio: false,
  };

export const DEFAULT_FILE_NAME = 'video-recording';


